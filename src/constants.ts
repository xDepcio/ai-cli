export const STORE_DIR_PATH = `${process.env.HOME}/.ai-cli` as const
// export const DEFAULT_CONFIG_FILE_PATH = `${DEFAULT_DIR_PATH}/config.json` as const
export const KEYPRESS_TO_COMPLETION_FETCH_TIMEOUT = 150 as const
